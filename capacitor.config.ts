import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'treepad-cloud-enterprise-client',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
